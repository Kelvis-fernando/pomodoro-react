import { useInterval } from './use-interval';

describe('Set intervals for the function', () => {
  it('should be calleds the const savedCallBack', () => {
    expect(useInterval).toBeTruthy();
  });
});
